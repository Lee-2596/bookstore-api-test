# 🚀 速查卡片

> 把这张卡片打印出来放在电脑旁，遇到问题随时查。

---

## 一、启动流程（每天开机的第一件事）

```bash
# 1. 打开 cmd 窗口 A（保持运行，不能关）
cd "d:\杂七杂八\西安实训\项目\0918\00-项目源码"
.\venv\Scripts\Activate.ps1
python app.py

# 2. 验证服务
curl http://127.0.0.1:5000/api/health

# 3. 打开 cmd 窗口 B（这个用来跑测试）
cd "d:\杂七杂八\西安实训\项目\0918\08-Pytest自动化"
.\venv\Scripts\Activate.ps1  # 注意：每个新窗口都要重新激活
python run.py all
```

---

## 二、常用命令速查

### Windows / Python
```bash
# 路径操作
cd <路径>           # 切目录
dir                # 列文件（cmd）
ls                 # 列文件（PowerShell）
mkdir <目录>       # 建目录

# Python 环境
python --version              # 版本
python -m venv venv           # 建虚拟环境
.\venv\Scripts\Activate.ps1   # 激活（PowerShell）
venv\Scripts\activate.bat     # 激活（cmd）
deactivate                    # 退出虚拟环境

# 包管理
pip install <包名>            # 装包
pip install -r requirements.txt  # 批量装
pip list                      # 已装包
pip freeze > requirements.txt # 导出
```

### Linux (WSL)
```bash
ls -la             # 列文件（详细）
cd ~               # 回到家目录
pwd                # 当前路径
cat 文件           # 看文件
tail -f 文件       # 跟踪日志
grep '关键词' 文件  # 搜索
ps aux | grep 进程名   # 找进程
kill PID           # 杀进程
lsof -i :端口号    # 看端口占用
```

### MySQL
```bash
mysql -u root -p bookstore    # 进入 MySQL

# SQL
SHOW DATABASES;
SHOW TABLES;
DESC 表名;                       # 看表结构
SELECT * FROM 表 LIMIT 10;
UPDATE 表 SET 字段=值 WHERE ...;
DELETE FROM 表 WHERE ...;
```

### Git
```bash
git init                       # 初始化仓库
git add .                      # 暂存所有变更
git commit -m "提交说明"       # 提交
git status                     # 看状态
git log --oneline              # 看历史
git remote add origin <url>    # 关联远程
git push -u origin main        # 推送到远程
```

---

## 三、BookStore API 速查

| 接口 | 方法 | 鉴权 | 说明 |
|------|------|------|------|
| /api/health | GET | ❌ | 健康检查 |
| /api/auth/register | POST | ❌ | 注册 |
| /api/auth/login | POST | ❌ | 登录拿 Token |
| /api/auth/profile | GET | ✅ | 我的信息 |
| /api/categories | GET | ❌ | 分类列表 |
| /api/categories | POST | ✅管理员 | 新建分类 |
| /api/books | GET | ❌ | 商品列表 |
| /api/books/{id} | GET | ❌ | 商品详情 |
| /api/books | POST | ✅管理员 | 新建商品 |
| /api/books/{id} | PUT | ✅管理员 | 修改商品 |
| /api/books/{id} | DELETE | ✅管理员 | 下架商品 |
| /api/orders | POST | ✅ | 下单 |
| /api/orders | GET | ✅ | 我的订单 |
| /api/orders/{id}/pay | POST | ✅ | 支付订单 |

### 业务错误码
| code | 含义 |
|------|------|
| 200 | 成功 |
| 4001 | 参数为空 |
| 4002 | 用户名长度错 |
| 4003 | 密码长度错 |
| 4004 | 用户名已存在 |
| 4005 | 邮箱已注册 |
| 4006 | 用户名或密码错误 |
| 4007 | 账号已禁用 |
| 4008 | 分类已存在 |
| 4009 | 商品已下架 |
| 4010 | 库存不足 |
| 4011 | 订单状态错 |
| 4031 | 无权操作 |
| 4041 | 资源不存在 |
| 5001 | 服务器错误 |

---

## 四、Pytest 速查

```bash
# 跑测试
pytest                                  # 跑全部
pytest cases/test_auth.py               # 跑某个文件
pytest cases/test_auth.py::TestAuth::test_login_success  # 跑某个用例
pytest -v                               # 详细输出
pytest -k "login"                       # 按名称匹配
pytest -m smoke                         # 按标记跑（冒烟）
pytest -m "p0 and not smoke"            # 组合标记
pytest --html=reports/report.html       # 生成 HTML 报告
pytest --alluredir=reports/allure_raw   # 生成 Allure 数据
pytest -x                               # 失败即停
pytest --tb=short                       # 简短错误信息

# Allure 报告
allure generate reports/allure_raw -o reports/allure_html --clean
allure open reports/allure_html
```

---

## 五、调试技巧

### Python 报错看不懂？
1. 复制错误信息最后一行（File "...", line XX）
2. 用 Read 工具打开那个文件，跳到那一行
3. 看错误类型（KeyError / TypeError / AttributeError）

### 接口调不通？
1. 先用 Postman 调通
2. 把 Postman 的 Headers 和 Body 复制到 Python
3. 看响应状态码和 message

### MySQL 连不上？
1. 服务是否启动：services.msc → MySQL80
2. 端口是否对：telnet 127.0.0.1 3306
3. 用户名密码是否对
4. 数据库是否存在：SHOW DATABASES

### Pytest 找不到用例？
1. 文件名是否 test_*.py
2. 函数名是否 test_*
3. 类名是否 Test* 且没有 __init__
4. 是否在 testpaths 指定的目录

### Allure 报告空白？
1. 是否先跑 `pytest --alluredir=...` 生成数据
2. 再跑 `allure generate ...` 转 HTML
3. 浏览器打开 `reports/allure_html/index.html`

---

## 六、紧急情况

### 项目突然跑不起来？
```bash
# 1. 重新装依赖
pip install -r requirements.txt --force-reinstall

# 2. 数据库重置
mysql -u root -p -e "DROP DATABASE bookstore; CREATE DATABASE bookstore DEFAULT CHARACTER SET utf8mb4;"
python init_db.py

# 3. 虚拟环境重建
rmdir /s /q venv
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

### 端口 5000 占用？
```bash
# Windows
netstat -ano | findstr :5000
taskkill /PID <进程ID> /F
```

### 完全卡住怎么办？
1. 关掉所有 Python 进程：`taskkill /IM python.exe /F`
2. 重启电脑
3. 从头开始

---

## 七、关键路径

```
项目根目录     d:\杂七杂八\西安实训\项目\0918
后端代码       0918\00-项目源码\
自动化项目     0918\08-Pytest自动化\
测试数据文件   0918\00-项目源码\bookstore.db（SQLite）
MySQL 数据库  bookstore（库名）
Allure 报告    0918\08-Pytest自动化\reports\allure_html\index.html
```

---

## 八、面试必背的 5 个项目亮点

1. **项目自建**：从零搭建 BookStore 电商 API，不是用现成的，深度掌握架构
2. **数据驱动**：YAML 管理 8 条登录用例，参数化执行，新增场景不用改代码
3. **数据库断言**：下单事务回滚的验证，通过直接查 MySQL 验证业务一致性
4. **Allure 报告**：Feature/Story 分层，支持附日志，HR/面试官一眼能看懂
5. **工程化**：fixture 共享登录态、一键运行脚本、P0/P1 标记分级——真正能落地的工程实践