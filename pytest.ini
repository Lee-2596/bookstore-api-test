[pytest]
testpaths = cases
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    smoke: 冒烟测试
    p0: P0 级别用例（核心）
    p1: P1 级别用例（重要）
    p2: P2 级别用例（一般）